# Hozircha frontend uchun majburiy environment o'zgaruvchi yo'q.
# Backend manzili dev rejimida vite.config.ts orqali proksi qilinadi (/api -> localhost:8787),
# production'da esa deploy platformasida "VITE_API_BASE" kabi o'zgaruvchi kerak bo'lsa qo'shiladi.
